#!/bin/bash
# The multiworld (dimensions) system is now part of the source code
# (src/dimension.*, src/server.*, builtin/game/dimensions.lua ...),
# so there is nothing left to patch. The file is kept only because the
# Android workflow still calls it.
echo "Multiworld: already built into the source, nothing to apply."
